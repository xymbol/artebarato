<nav>
  <%= link_to root_url, class: "brand" do %>
    <span>Arte Barato</span>
  <% end %>

  <input id="bmenub" type="checkbox" class="show">
  <label for="bmenub" class="burger pseudo button">&#9776;</label>

  <div class="menu">
    <% navigable_categories.each do |category| %>
      <%= link_to category.name, category_events_url(category), class: "button pseudo" %>
    <% end %>
  </div>
</nav>
